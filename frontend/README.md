# Frontend

Self-contained React dashboard. Scoring and SHAP-style drivers run in the browser, so the app deploys on Vercel as a static site.

```sh
npm install
npm run dev
```

Open `http://localhost:5173`. The FastAPI backend is optional and only needed if you want the original trained model locally.
